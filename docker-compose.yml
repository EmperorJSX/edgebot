services:
  postgres:
    image: postgres:18-alpine
    environment:
      POSTGRES_USER: edgebot
      POSTGRES_PASSWORD: edgebot
      POSTGRES_DB: edgebot
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data

  redis:
    image: redis:8-alpine
    command: redis-server --appendonly yes
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  pg_data:
  redis_data:
